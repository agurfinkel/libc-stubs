#include "sea_dsa.h"

int bcmp(const void *s1, const void *s2, size_t n) {
sea_dsa_set_read(s1);
sea_dsa_set_read(s2);
int retVal = 0;
sea_dsa_collapse(const void *s1);
sea_dsa_collapse(const void *s2);
return retVal;

}