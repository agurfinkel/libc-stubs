#include "sea_dsa.h"

int vsnprintf(char *restrict s, size_t n, const char *restrict fmt, va_list ap) {
sea_dsa_set_read(n);
sea_dsa_set_modified(n);
sea_dsa_set_read(fmt);
int retVal = 0;
return retVal;

}